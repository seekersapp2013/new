import React from 'react';
export interface ScreenHeaderProps {
    appDetails?: {
        name: string;
        icon: string;
    };
    title?: string | JSX.Element;
    rightContent?: React.FC | JSX.Element;
    close?: () => void;
    hideIcon?: boolean;
    hideLogo?: boolean;
}
export declare const ScreenHeader: ({ appDetails, hideLogo, rightContent, title: _title, ...rest }: ScreenHeaderProps) => JSX.Element;

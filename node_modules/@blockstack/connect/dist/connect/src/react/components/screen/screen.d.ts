import React from 'react';
import { FlexProps } from '@blockstack/ui';
interface ScreenBodyProps extends FlexProps {
    noMinHeight?: boolean;
    isLoading?: boolean;
    onSubmit?: () => void;
}
export declare const Screen: React.FC<ScreenBodyProps>;
export {};

import React from 'react';
import { BoxProps } from '@blockstack/ui';
interface AppIconProps {
    src: string;
    alt: string;
}
declare const AppIcon: React.FC<AppIconProps & BoxProps>;
export { AppIcon };
